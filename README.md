INSTALL : the binary file iForm is ready to use,use command "chmod 777 iForm" to get the permission
USAGE: iForm [options] <motif file> <sequence file>
More information is avaliable by using parameter -h
